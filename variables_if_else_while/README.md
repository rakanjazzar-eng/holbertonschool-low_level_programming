# C - Variables, if, else, while

This project introduces the basics of variables, data types, arithmetic operators, relational and logical operators, conditional statements (`if`, `if...else`), and loops (`while` and `do...while`) in the C programming language.